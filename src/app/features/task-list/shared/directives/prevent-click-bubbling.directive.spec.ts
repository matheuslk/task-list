import { PreventClickBubblingDirective } from './prevent-click-bubbling.directive';

describe('PreventClickBubblingDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventClickBubblingDirective();
    expect(directive).toBeTruthy();
  });
});
